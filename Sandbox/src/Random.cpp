#include "Random.h"

std::mt19937 Random::s_RandomEngine; // Definition of the static member variable.
// Note: The actual methods (Init, Float, Int) are implemented inline within the class definition and thus do not need separate definitions in this .cpp file.
